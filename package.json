{
  "name": "aerofatigue",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp standalone.html dist/ 2>/dev/null || true && cp -r assets dist/ 2>/dev/null || true && cp _redirects dist/ 2>/dev/null || true"
  }
}